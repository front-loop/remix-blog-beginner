generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./data.db"
}

model Post {
  id         String   @id
  title      String
  content    String
  created_at DateTime @default(now())
}
